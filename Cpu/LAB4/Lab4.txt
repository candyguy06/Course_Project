這次的LAB主要是要叫我們利用MIPS指令完成中斷的功能，我的理解是把需要的東西存起來之後，
等中斷結束再把原本存的東西reload回來，第一個部分是備份CP0_Epc, CP0_Status, CP0_Cause先暫存起來，
等中斷結束後再reload回來，但是這裡的step2有提到要修改CP0_Status到enable interrupt的狀態後跳回EPC的位置，
這邊我不確定自己寫的對不對，第二部分就是要叫我們儲存$1~$31的資料，等中斷結束後在RELOAD回來。
